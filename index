<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>AI Art Diary 🎨 | Culaliya</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    :root {
      --neon-blue: #0ff;
      --deep-space: #06090f;
      --accent: #ff3eff;
    }
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      background: radial-gradient(circle at 50% 30%, #101624 0%, #06090f 100%);
      color: white;
      font-family: 'Orbitron', sans-serif;
      overflow: hidden;
    }
    h1 {
      font-size: 3em;
      color: var(--neon-blue);
      text-shadow: 0 0 20px var(--neon-blue);
      letter-spacing: 2px;
      margin-bottom: 0.4em;
      animation: glow 3s ease-in-out infinite alternate;
    }
    @keyframes glow {
      from { text-shadow: 0 0 10px var(--neon-blue), 0 0 20px var(--neon-blue); }
      to { text-shadow: 0 0 25px var(--accent), 0 0 50px var(--neon-blue); }
    }
    p {
      font-size: 1.1em;
      max-width: 600px;
      text-align: center;
      color: #cfd9e6;
      line-height: 1.6;
      margin: 0.4em 0;
    }
    .highlight {
      margin-top: 2em;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.4em;
      font-size: 1em;
    }
    .highlight span {
      background: linear-gradient(90deg, var(--neon-blue), var(--accent));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      letter-spacing: 1px;
    }
    footer {
      position: absolute;
      bottom: 20px;
      font-size: 0.8em;
      color: #666;
    }
    canvas {
      position: fixed;
      top: 0;
      left: 0;
      z-index: -1;
    }
  </style>
</head>
<body>
  <canvas id="stars"></canvas>
  <h1>AI ART DIARY ⚡</h1>
  <p>Exploring the edge between algorithm and imagination.<br>
     A living archive of <strong>AI-generated music, art, and emotions</strong>.</p>
  <div class="highlight">
    <span>🧩 SwiftUI Projects</span>
    <span>🎵 Suno Music Prompts</span>
    <span>🦙 Midjourney Concept Art</span>
  </div>
  <footer>
    <a href="https://github.com/Culaliya/ai-art-diary" style="color:#888;text-decoration:none;">View Source on GitHub</a>
  </footer>

  <script>
    const canvas = document.getElementById('stars');
    const ctx = canvas.getContext('2d');
    let stars = [];
    function resize() {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
      stars = Array.from({length: 200}, () => ({
        x: Math.random()*canvas.width,
        y: Math.random()*canvas.height,
        r: Math.random()*1.5,
        o: 0.2+Math.random()*0.8
      }));
    }
    function draw() {
      ctx.clearRect(0,0,canvas.width,canvas.height);
      ctx.fillStyle = 'rgba(255,255,255,0.8)';
      for (let s of stars) {
        ctx.beginPath();
        ctx.arc(s.x,s.y,s.r,0,Math.PI*2);
        ctx.fillStyle = `rgba(255,255,255,${s.o})`;
        ctx.fill();
      }
    }
    function animate() {
      for (let s of stars) {
        s.y += 0.05;
        if (s.y > canvas.height) s.y = 0;
      }
      draw();
      requestAnimationFrame(animate);
    }
    window.addEventListener('resize', resize);
    resize();
    animate();
  </script>
</body>
</html>
